#include <stdio.h>
#include <string.h>

int main() {
    char str[] = "Hello, World!"; 

    
    int length = strlen(str);
    printf("Length of the string: %d\n", length);

   
    int size = sizeof(str);
    printf("Size of the string: %d\n", size);

    return 0;
}
